#!/usr/local/bin/bash
[ -z "$PS1" ] && return

. ~/.bashrc.d/settings
. ~/.bashrc.d/paths
. ~/.bashrc.d/colors
. ~/.bashrc.d/greeting
. ~/.bashrc.d/aliases
. ~/.bashrc.d/functions
. ~/.bashrc.d/prompt
. ~/.bashrc.d/completion

